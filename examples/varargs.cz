let x = fn() {
    print(...);
};

x(1);
x(1, 2);
x("hi", 10.1, {"a": "b"});

let y = fn () {
    # TODO: if the ... literal is changed to just return
    # a regular array, the array_from global can go away
    print(array_from(...))
}
y(1, 2, 3)
