let name = "/tmp/cozy-file-write-test";
let fh = fs.open(name, "w");

# Now we have a handle we can write (strings) to it:
fh.write("This is a test");
fh.write(string({"foo": "bar", "baz": 8}));

# And close it.
fh.close();

# Appending
let fh2 = fs.open(name, "wa");
fh2.write("\nanother line");
fh2.close();