# WIP
let r = http.client();
let res = r("https://example.com");
let resp = r(
    "https://some.api",
    {
        "method": "POST",
        "body": "[1,2,3]",
        "headers": {
            "x-foo": "bar"
        }
    }
);
print(json.deserialize(resp))
