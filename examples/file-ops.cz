exit(0)
# Exiting early so you don't actually run this

fs.chmod("/tmp/executable.sh", "755")
fs.chmod("/tmp/normal", "644")

# remove file
fs.unlink("/tmp/trash.txt");

fs.mkdir("/tmp/blah");

fs.stat("./README.md")
