exit(0)
# Exiting early so you don't actually run this

chmod("/tmp/executable.sh", "755")
chmod("/tmp/normal", "644")

# remove file
unlink( "/tmp/trash.txt");

mkdir("/tmp/blah");

stat("./README.md")
