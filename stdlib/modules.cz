let import = fn(path, name) {
    mutable p = path;
    if (!match(".cz$", p)) {
        p = p + ".cz"
    }
    let contents = open(p, "r").lines().join("");
    # print(contents);
    return { name: eval(contents) };
};

# TODO: this isn't quite right, we really want to name the module what the let
# identifier says, or failing that, put it in scope with the second argument.
# let fb = import("../examples/fizzbuzz", "fb");
# print(fb["fb"]());
